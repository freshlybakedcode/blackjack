import assert from 'assert';
import deal from '../../src/js/modules/deal';

describe('deal.js tests', function () {
  it('should exist', function () {
    assert.notStrictEqual(deal, undefined);
  });
});

